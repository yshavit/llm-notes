[package]
name = "simpllm"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1.0"
tiktoken-rs = "0.9.1"
serde = { version = "1.0", features = ["derive"] }

[dev-dependencies]
approx = "0.5" # Use the latest version
