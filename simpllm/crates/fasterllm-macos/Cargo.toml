[package]
name = "fasterllm-macos"
version = "0.1.0"
edition = "2024"

[dependencies]
fasterllm = { path = "../fasterllm" }
candle-core = { version = "0.9", default-features = false, features = ["metal"] }
