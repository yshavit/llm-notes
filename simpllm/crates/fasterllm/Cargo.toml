[package]
name = "fasterllm"
version = "0.1.0"
edition = "2024"

[features]
cuda = ["candle-core/cuda"]

[dependencies]
candle-core = { workspace = true }
candle-nn = "0.9"
simpllm = { path = "../simpllm" }

