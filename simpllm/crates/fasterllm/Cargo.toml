[package]
name = "fasterllm"
version = "0.1.0"
edition = "2024"

[features]
cuda = ["candle-core/cuda"]
metal = ["candle-core/metal"]

[dependencies]
candle-core = "0.9"
candle-nn = "0.9"
simpllm = { path = "../simpllm" }
simpllm-core = { path = "../../simpllm-core" }

