[package]
name = "simpllm-core"
version = "0.1.0"
edition = "2024"

[dependencies]
rand = "0.10.0" # for sampling logits
rayon = "1.11.0" # multithreading
serde_json = { workspace = true } # config and TUI stuff

[dev-dependencies]
approx = "0.5"
tiktoken-rs = "0.9.1"
